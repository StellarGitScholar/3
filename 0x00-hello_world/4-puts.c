#include <stdio.h>

/**
 * main - a simple c program to print with the pits command
 *
 * puts: command to print to stdout
 *
 * Return: 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
